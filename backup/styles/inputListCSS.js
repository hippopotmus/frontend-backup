import styled from "styled-components";
import { sf } from "./style-functions";

export const InputListSection = styled.section`
  ${sf.setFlex({ gap: "2em", jc: "flex-start", fw: "wrap" })}
  .project_list_btns {
  }
`;
